server:
  port: 8761  #当前Eureka服务的端口
eureka:
  client:
    register-with-eureka: false  #是否将自己作为服务注册
    fetch-registry: false  #是否同步其它的注册中心的数据
    service-url:
      defaultZone: http://localhost:8761/eureka/  #注册中心的访问地址
